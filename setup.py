from setuptools import setup

setup(
   name='sasi ayyagari',
   version='1.0',
   description='A basic calculator code',
   author='Sasi kumar AVVK',
   author_email='foomail@foo.com',
   keywords='calculator',
   packages=['sasi ayyagari'],  #same as name
   install_requires=[' '], #external packages as dependencies
)